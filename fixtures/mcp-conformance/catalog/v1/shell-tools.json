{
  "fixtureVersion": 1,
  "caseID": "catalog.shell.sdk-emitted.v1",
  "catalogKind": "sdk-emitted",
  "server": {
    "name": "workcell-shell",
    "version": "0.1.0"
  },
  "provenance": [
    "rust/crates/mcp-shell/src/catalog.rs",
    "rust/crates/mcp-shell/src/group.rs"
  ],
  "expected": {
    "tools": [
      {
        "name": "shell",
        "title": "Execute shell command",
        "description": "Execute a Bash command on the MCP server host.\n\nUsage notes:\n- The command parameter is required.\n- Commands must be valid MCP JSON strings, are bounded to 65536 UTF-8 bytes, and are authorized by immutable operator policy before execution. Malformed JSON or non-UTF-8 request payloads are rejected by the MCP transport before tool dispatch.\n- timeout is optional, measured in milliseconds, defaults to 120000, and is capped at 600000.\n- Use workdir instead of embedding cd commands. It must resolve inside the configured root and defaults to \".\".\n- Only the initial working directory is root-confined. Execution is unsafe and unsandboxed: commands can mutate host files, access the network, and read inherited environment variables.\n- Prefer the dedicated file tools when they are available and fit the operation, and prefer the code execution tool for pure computation such as arithmetic, statistics, string processing, and JSON reshaping, because it runs isolated from the host.\n- To read part of a file, use file_read with offset and limit rather than sed, awk, head, or tail.\n- Always quote file paths that contain spaces.\n- Non-zero exits are completed results with an exit code so the caller can inspect and continue.\n- Output is streamed through MCP progress notifications; the final result contains bounded tails and completion accounting.\n- The result is already bounded per stream and, by default, reduced by a built-in filter that keeps errors, warnings, and summaries. Run the command directly instead of piping into head or tail: blind truncation defeats that filter entirely, because it applies only to a single-program command, and it withholds live output until the process exits. Piping into rg or grep to search output is fine.\n- Prefer separate streams to 2>&1. Merged output is filtered as stdout, so stderr diagnostics become subject to stdout line caps instead of being carried in full.\n- Background execution is unsupported; descendants that retain output pipes are terminated.",
        "inputSchema": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "command": {
              "type": "string",
              "minLength": 1,
              "description": "Bash command to execute on the MCP server host."
            },
            "timeout": {
              "type": "integer",
              "minimum": 1,
              "maximum": 600000,
              "default": 120000,
              "description": "Optional timeout in milliseconds. Defaults to 120000 and is capped at 600000."
            },
            "workdir": {
              "type": "string",
              "minLength": 1,
              "description": "Optional configured-root-relative or absolute initial working directory inside the configured root."
            }
          },
          "required": [
            "command"
          ],
          "$schema": "http://json-schema.org/draft-07/schema#"
        },
        "outputSchema": {
          "type": "object",
          "properties": {
            "version": {
              "type": "integer",
              "format": "uint8",
              "minimum": 0,
              "maximum": 255,
              "description": "Version of the structured result shape, independent of the MCP protocol version."
            },
            "kind": {
              "type": "string"
            },
            "relativeWorkdir": {
              "type": "string"
            },
            "timeoutMs": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "durationMs": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "exitCode": {
              "type": [
                "integer",
                "null"
              ],
              "format": "int32"
            },
            "signal": {
              "type": [
                "integer",
                "null"
              ],
              "format": "int32"
            },
            "timedOut": {
              "type": "boolean"
            },
            "outputLimitExceeded": {
              "type": "boolean"
            },
            "finalSequence": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0,
              "description": "Last emitted progress sequence, or zero when the command produced no decoded output."
            },
            "stdoutUtf8Bytes": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0,
              "description": "UTF-8 bytes emitted in stdout progress chunks, independent of raw process byte counts."
            },
            "stderrUtf8Bytes": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0,
              "description": "UTF-8 bytes emitted in stderr progress chunks, independent of raw process byte counts."
            },
            "stdout": {
              "type": "string"
            },
            "stderr": {
              "type": "string"
            },
            "stdoutCaptureTruncated": {
              "type": "boolean"
            },
            "stderrCaptureTruncated": {
              "type": "boolean"
            },
            "stdoutPreviewTruncated": {
              "type": "boolean"
            },
            "stderrPreviewTruncated": {
              "type": "boolean"
            },
            "stdoutRedrawsCollapsed": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0,
              "description": "Redraw frames absorbed while rendering stdout as a terminal would show\nit. Non-zero means the capture held a progress bar that overwrote itself;\n`stdout_utf8_bytes` still reports what the command actually wrote."
            },
            "stderrRedrawsCollapsed": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0,
              "description": "The same for stderr, where bars are more commonly written."
            }
          },
          "required": [
            "version",
            "kind",
            "relativeWorkdir",
            "timeoutMs",
            "durationMs",
            "timedOut",
            "outputLimitExceeded",
            "finalSequence",
            "stdoutUtf8Bytes",
            "stderrUtf8Bytes",
            "stdout",
            "stderr",
            "stdoutCaptureTruncated",
            "stderrCaptureTruncated",
            "stdoutPreviewTruncated",
            "stderrPreviewTruncated",
            "stdoutRedrawsCollapsed",
            "stderrRedrawsCollapsed"
          ],
          "title": "ShellOutput",
          "$schema": "http://json-schema.org/draft-07/schema#"
        },
        "annotations": {
          "readOnlyHint": false,
          "destructiveHint": true,
          "idempotentHint": false,
          "openWorldHint": true
        },
        "_meta": {
          "ai.workcell/presentation-profile": "shell.result.v1",
          "ai.workcell/contract": {
            "id": "shell.execution.v1",
            "version": "v1",
            "resultVersion": "v1"
          }
        }
      }
    ]
  }
}
